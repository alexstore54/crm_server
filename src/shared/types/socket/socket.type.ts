export enum SocketRooms {
  dashboard = 'dashboard',
}
export enum SocketNamespaces {
  auth = 'auth',
}
export enum SocketEvents {
  connect = 'connect',
  disconnect = 'disconnect',
  error = 'error',
  auth = 'auth',
}